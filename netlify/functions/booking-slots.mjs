const json=(body,status=200)=>new Response(JSON.stringify(body),{status,headers:{"content-type":"application/json; charset=utf-8","cache-control":"no-store"}});
export default async(request)=>{if(request.method!=="GET")return json({ok:false,error:"Method not allowed"},405);const webhookUrl=Netlify.env.get("GOOGLE_SHEETS_WEBHOOK_URL"),webhookSecret=Netlify.env.get("GOOGLE_SHEETS_WEBHOOK_SECRET");if(!webhookUrl||!webhookSecret)return json({ok:false,error:"Booking service is not configured"},503);try{const response=await fetch(webhookUrl,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({secret:webhookSecret,action:"availability"})});const result=await response.json().catch(()=>({ok:false}));if(!response.ok||!result.ok)return json({ok:false,error:"Online scheduling is not available yet"},503);return json({ok:true,timezone:result.timezone,owner:result.owner,duration_minutes:result.duration_minutes,buffer_minutes:result.buffer_minutes,days:result.days||[]});}catch{return json({ok:false,error:"Unable to check availability"},503);}};
export const config={path:"/api/booking-slots"};
